apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: orders-postgres-volume-claim
  labels:
    app: orders-postgres
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
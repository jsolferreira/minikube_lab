apiVersion: v1
kind: Service
metadata:
  name: {{ include "mtapp.fullname" . }}
  labels:
    {{- include "mtapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mtapp.selectorLabels" . | nindent 4 }}
